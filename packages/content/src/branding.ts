// The title is provisional (CLAUDE.md). It lives here, in exactly one place —
// never hard-code the game's name anywhere else in the codebase.
export const GAME_TITLE = "Veilbreak: Arena of the Fallen";
