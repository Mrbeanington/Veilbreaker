// packages/persistence: IndexedDB storage, versioned save schema with
// migrations, export/import (spec/06 "Persistence and profile"). Built out
// in Phase 09 — this is a Phase 00 placeholder so the workspace package
// exists and other packages can depend on it.
export const PERSISTENCE_PACKAGE_PLACEHOLDER = true;
