# Phase 08 — Full client UI
**Read first:** spec/05 (all)

## Deliverables
Main navigation (PLAY, CHARACTERS, TEAMS, RANKED placeholder, CODEX, MISSIONS placeholder, LEGENDS, PROFILE placeholder, SETTINGS). Self-hosted fonts and icons only. Character select with all filters, favorites, and team presets. The Codex with knowledge levels (discovery state persisted to IndexedDB through `packages/persistence`). The **Legend Chamber** as a non-grid 12-position design that evolves with unlocks, with the Nameless One in the ultimate position. The full accessibility settings list. Sound-event hooks with a silent default.

## Acceptance criteria
Keyboard-navigable throughout. Secrets are hidden correctly. Codex entries unlock from the player's own match event history.

Finish with the end-of-phase report.
