# Open Questions

The source spec leaves these ambiguous. Each has a **proposed default** so work can proceed. The project owner may override any default; when that happens, record the change in DECISIONS.md.

| ID | Question | Proposed default |
|---|---|---|
| OQ-01 | **Simultaneous vs. alternating turns.** The spec asks for simultaneous planning but also wants "first-turn advantage" analytics, and classic team battlers alternate. | Build the resolver so both work: a `TurnModel` config with `simultaneous` (default) and `alternating`. Under simultaneous play, "first-turn advantage" is measured as the initiative-holder's advantage (OQ-02). |
| OQ-02 | Within the same resolution tier, whose action resolves first? | Initiative alternates each turn (a coin-flip seeded from RNG decides turn 1). Within a player, order follows the ally slot (1→3). Always deterministic and shown in the UI. |
| OQ-03 | Default energy generation. | 1 random energy per living character per turn, with a pool cap of 10 per type, full carryover, and no energy on turn 1 for the initiative player. All values configurable. |
| OQ-04 | Does life transfer (Borrowed Life) bypass anti-heal or "cannot be healed" (Behemoth)? | Healing has classes: `heal`, `lifeTransfer`, `setHp`. Anti-Heal and Behemoth's passive block `heal` only; Healing Reduction affects both `heal` and `lifeTransfer`. Log the decision. |
| OQ-05 | Legends are stronger and unlocked by accomplishment. How should they work in local ranked and friend matches? | ~~Local ranked: bots in lower divisions use at most 1 Legend per team; the player may use any Legends they've unlocked. Friend matches: the host chooses "own unlocks" or "everything unlocked" with an optional max-1-Legend rule. Revisit at Phase 11 using simulation data.~~ **Resolved (Phase 11, ADR-020):** bots below Platinum bring at most 1 Legend, Platinum and above at most 2; the player may use any unlocked Legend; Legends stay locked in ranked even with the spoiler switch. Friend-match rules were settled in Phase 10. |
| OQ-06 | **Nameless One rewind semantics.** | When he would die, restore the full snapshot from the start of the current turn (both teams). The same actions do not replay; both players re-plan. The RNG stream advances (no identical re-roll). Once per battle, and consumed even if the rewound turn kills him again. |
| OQ-07 | Behavior when a Cheater changes targets "after selection" under simultaneous planning. | Implement retargeting as a Priority-tier effect that modifies queued actions, and log the change. |
| OQ-08 | Timer expiry with no lock-in. | Unlocked actions are dropped (character passes). In friend matches, the timer is advisory, since async turns have no shared clock; a player may resign or abandon. In local ranked against bots, the timer is optional in settings. |
| OQ-09 | Simultaneous team wipe. | Draw. In ranked, a draw counts as neither a win nor a loss, with a small MMR adjustment toward parity. |
| OQ-10 | **"MICHELIN MONSTER" uses a registered trademark.** | **Resolved in phase 13, region 10:** Chef Ramble's form is now "The Five-Star Fiend" (transformation `transformation.chef-ramble.to-the-five-star-fiend`). |
| OQ-11 | "Devourer of Worlds" has a well-known comic-book association. | Low risk as a generic phrase; flag it for legal review before launch. |
| OQ-12 | "Each culture must receive specific research before final visual production." Claude Code cannot substitute for cultural consultants. | Art specs are marked `status: draft`. Final production requires human review, ideally by people from those cultures. |
| OQ-13 | Hidden mechanics vs. ranked integrity. | Every effect that changes state emits a log event, even hidden ones; the event text may be cryptic but must exist. Codex entries unlock from the player's own event history. |
| OQ-14 | Maximum match length. | 40 turns; then the team with the higher total remaining HP percentage wins; if tied, draw. |
| OQ-15 | Mirror matches / duplicate characters. | No duplicates within a team; mirrors across teams are allowed. |
| OQ-16 | Players can edit local saves to unlock everything. | Accepted (ADR-002). Keep the checksum to detect accidental corruption only, and do not attempt obfuscation-based anti-cheat. |
| OQ-17 | Saves from older builds after balance/content changes. | Saves store IDs and progress, never character numbers. Migrations handle renamed or removed IDs. Every shipped BalanceVersion stays in the build for replays. |
| OQ-18 | Live WebRTC needs STUN/TURN servers to cross most home networks. | Stretch goal only, LAN-guaranteed. Public STUN is optional and off by default; code matches are the supported path. |
| OQ-19 | Browsers may evict IndexedDB data under storage pressure (especially Safari's ~7-day rule for non-installed sites). | The primary mitigation is installing as an app early, plus `storage.persist()`, rolling backups, and an easy QR transfer and share-sheet backup. |
| OQ-20 | Hosting. | Static host of the owner's choice. Also produce an optional single-file build for local/offline distribution. |
| OQ-21 | Art assets without an image API at runtime. | Art is produced offline from the CharacterArtSpecs and shipped as static, optimized images (WebP/AVIF) with lazy loading by region. Generated placeholder portraits are used until then. |
| OQ-22 | On iOS, installed home-screen apps have separate storage from Safari, and links/camera scans open Safari rather than the app. | Prompt install before real play; offer "Move my progress into the app"; iOS transfers use the in-app scanner or paste, not links. |
| OQ-23 | The transfer payload could outgrow a single QR code as content expands. | Budget ≤ 2,500 bytes compressed, tested against a maxed profile. The fallback is an animated multi-frame QR sequence, then the share-sheet file. |
| OQ-24 | A service-worker update could break an old save. | Migrations run before any save write in a new version, and the rolling backup is taken before migrating. |
| OQ-25 | Phase 00 requires zod schemas for `BattleState`, but full per-character runtime fields (HP, active statuses, cooldowns, live resource values) depend on the resolver design, which is explicitly out of scope for Phase 00. | Ship `BattleState` as a foundational skeleton now (format, teams, RNG state, energy pools, event log) and extend it with per-character runtime state in Phase 01 when the resolver is built. This is additive, not a breaking change, since Phase 00 ships no persisted battle states yet. |
| OQ-26 | ~~`apps/web/index.html`'s strict CSP meta tag was unverified against Vite's dev server (HMR websocket, eval-based dev tooling).~~ **Resolved 2026-09-17:** verified in the browser pane against both `vite dev` and `vite preview` (the production build) — the app renders, `document.title` updates to `GAME_TITLE`, HMR's websocket connects, and no CSP violations appear in the console. | No dev-only relaxation needed; the CSP as written works in both modes. |
| OQ-27 | OQ-01's `TurnModel` config has two values, `"simultaneous"` (default) and `"alternating"`, but Phase 01's `resolveTurn(state, actionsA, actionsB, deps)` only implements simultaneous resolution — it takes both players' actions together. | Deferred until a game mode actually needs alternating turns (see ADR-007). Building it speculatively now, with no content or mode to exercise it, risks guessing wrong about what "alternating" should mean operationally (does each side get a full resolveTurn cycle, or just action submission?). Revisit when local hotseat or a PvE encounter (spec/06) specifically calls for it. |
| OQ-28 | ~~OQ-09's "simultaneous team wipe → draw" rule and full win/loss detection from a team wipe were not implemented in Phase 01.~~ **Resolved 2026-09-18 (Phase 02):** `resolveTurn` checks both teams after every tier; a wipe on both sides emits `matchEndedInDraw`, a wipe on one side emits `matchEndedByTeamWipe`, and either takes precedence over the max-turn tiebreak for that turn. | Tested in `packages/engine/src/resolver.test.ts` ("simultaneous team wipe is a draw"). |
| OQ-29 | ~~13 statuses from spec/02's initial library had no concrete mechanic (Curse, Fear, Petrification, Mark, Stealth, Exposed, Resurrection Lock, Infection, Soul Consecration, Death Prevention), and 3 more (Energy Lock, Energy Cost Increase, Ability Lock) needed a per-application parameter the ActiveStatus model couldn't express.~~ **Further resolved 2026-09-18 (Phase 04, ADR-011):** Patient Zero needed Infection for real (just `tickBehavior: "damageOverTime"`, no new engine code) and Malachar needed Soul Consecration for real (a `hasStatus` check gating his Souls passive, plus a direct block in the `resurrect` effect handler). 6 remain data-only: Curse, Fear, Petrification, Mark, Stealth, Exposed. | Same disposition as before for the remaining 6 — full, valid, stackable, dispellable StatusDefinitions with a plain tooltip noting no mechanic yet, revisited when a real character kit (Phase 06+) needs one. |
| OQ-30 | The `drainEnergy` effect's `grantToSelf` path adds the drained amount to the source player's pool without checking `EnergyRules.poolCap` — `applyEffect` (packages/engine/src/effects.ts) has no `EnergyRules` in scope. | Left uncapped for now; no current ability exercises it. If a future ability needs the cap enforced on a granted steal, thread `EnergyRules` through `EffectContext` at that point rather than guessing the right plumbing today. |
| OQ-31 | Several Phase 03 trigger-system pieces are simplified pending real content: (a) ~~a passive/status's reactive effects always target the event's subject, never a resolved `TargetRule`~~ **partially resolved 2026-09-18 (Phase 04, ADR-011):** `Trigger.effectTarget: "self"` lets an effect land on the trigger's holder instead of the event's subject (Malachar's Souls passive needs exactly this) — but a full resolved `TargetRule` (e.g. a cross-character "thorns" mechanic targeting "whoever attacked me", a specific ally, or a random enemy) still isn't supported, only the binary subject-vs-holder choice; (b) `hasTag` and `secretScript` conditions always return `false` (no CharacterDefinition lookup is wired into the engine, and no secret script is registered) — still true, and now also the reason Malachar's Corpse Command can't literally borrow a defeated character's ability (see OQ-36); (c) `onMatchStart` is schema'd but never fired by `createBattle`; (d) `onTurnStart`/`onTurnEnd` fire once per living character at the literal start/end of `resolveTurn` — a reasonable reading, not the only one; (e) `onHpThreshold` re-fires on every qualifying `onDamaged` event rather than edge-detecting a single crossing, so a passive gated on it can fire repeatedly while HP stays below the threshold. | Kept simple until a real character's passive needs the richer behavior — see docs/DECISIONS.md ADR-010/ADR-011 for the reasoning on each. (a) is now half-done; a full per-reactive-effect `TargetRule` is the next piece, when a character actually needs to hit someone other than the subject or the holder. |
| OQ-32 | Summons: only `occupiesSlot: false` ("attached") summons do anything right now — they absorb damage for their owner. `occupiesSlot: true` ("temporary fourth unit") summons exist as tracked HP-bearing entities but aren't independently targetable, don't act, and don't count toward "living character" totals (energy generation, team-wipe checks). | Deferred until a real "fourth unit" character (Malachar-style, spec/03) forces the design — building full independent-unit integration speculatively risked getting the team-size-dependent mechanics (OQ-03's energy generation, OQ-09's wipe check) wrong without a concrete case to test against. |
| OQ-33 | A Transformation's `changes.tags` and `changes.energyCostOverrides` (packages/content/src/schemas/transformation.ts) aren't applied to runtime state by `applyTransformation` (packages/engine/src/transformations.ts) — nothing in the engine currently reads a per-character tag list or a per-character ability-cost override. | Apply them once something needs to read them: tags once `hasTag` gets a real CharacterDefinition lookup (OQ-31), cost overrides once `getEffectiveCost` (actions.ts) is extended to check a per-character override map alongside Energy Cost Increase. |
| OQ-34 | (Phase 04) `Transformation.trigger` (a `Condition`) is never automatically evaluated by anything in `packages/engine` — a character only actually transforms when an ability, passive, or status effect explicitly lists `{ kind: "transformInto", transformationId }`. Patient Zero's and Malachar's transformations both work today only because their kits deliberately check the same condition themselves (a passive for Patient Zero's automatic evolution; an explicit ability cast for Malachar's Death King). | Keep `Transformation.trigger` as accurate documentation of intent (and a value a future dev-tools/UI display could read) until a character design specifically needs the engine to auto-fire a transformation with no matching ability/passive effect to trigger it — building that speculatively risks guessing wrong about whether it should check every turn, every event, or something else. |
| OQ-35 | (Phase 04) `TargetRule`-based ability targeting can never select a dead character — `resolveTargets`' candidate pool (packages/engine/src/targeting.ts) always filters to `alive`. This blocks any real ability from casting `resurrect` at a chosen dead ally through the normal validate → resolveTargets → applyEffect pipeline; Phase 03's `resurrect` tests only exercise `applyEffect` directly with hand-supplied target ids, and Malachar's "You Belong to Me" was deliberately redesigned around a `summon` instead of `resurrect` specifically to avoid this (docs/design/characters/malachar.md). | Add an opt-in `TargetRule.includeDead` (or equivalent) flag once a real character's kit needs to cast a genuine "resurrect this specific ally" ability — most likely a healer/support Legend in a later phase. Not built speculatively now since no Phase 04 kit needs it. |
| OQ-36 | (Phase 04) The engine still can't target, or query the state of, one *specific* dead character: a Condition's `"enemy"`/`"ally"` target refs only ever resolve to a living character (packages/engine/src/conditions.ts), and per-effect `TargetRule` overrides only support `{ side: "self" }` (ADR-011) — ally/random/lowestHp/etc. per-effect targeting still isn't implemented, since it would need a `BattleState` + `RngState` threaded into `EffectContext` to re-run real targeting logic. Together these are why Malachar's Corpse Command, Raise the Forgotten, and You Belong to Me are all approximations rather than literal "use/raise/bind THIS corpse" mechanics (docs/design/characters/malachar.md). | Revisit once a character genuinely needs either: (a) per-effect targeting beyond self (e.g., a lifesteal-style ability that heals a *different* ally, not just the caster), or (b) real corpse-identity tracking (e.g., a character whose kit depends on "the last enemy who died" specifically). Both are more machinery than any Phase 04 acceptance criterion required. |
| OQ-37 | (Phase 05) `packages/ai/src/simple-bot.ts`'s `decideSimpleBotActions` guesses "lethal" from a flat sum of an ability's top-level `damage` effect amounts versus the target's current HP — no mitigation (damage reduction, shields, invulnerability) and no real simulation. Good enough for "random legal action, prefers kills" (phase-05-local-playable.md's own bar), not a foundation to extend for real bot levels. | Build `packages/ai`'s real BEGINNER/INTERMEDIATE/ADVANCED/EXPERT ladder from scratch in Phase 07, using `snapshotBattleState`/`restoreFromSnapshot` (packages/engine) to actually simulate a candidate action's outcome rather than guessing from raw ability data — this stopgap is not meant to be incrementally upgraded into that. |
| OQ-38 | (Phase 05) `apps/web` has no React error boundary — an uncaught exception anywhere in the component tree (a malformed prop, a future bug) blanks the whole app with only a browser-console stack trace, no in-app recovery. | Add one in Phase 08 ("Full client UI"), when there's a real navigation shell to fall back to (a "something went wrong — return home" screen) rather than the single-screen state machine Phase 05 built. |
| OQ-39 | (Phase 05) `SettingsContext` (animation speed, turn-timer on/off) is session-only React state — reloading the page resets both to their defaults. | Move into `packages/persistence`'s settings store once Phase 09 builds it; no local-save system exists yet for these to live in. |
| OQ-40 | ~~(Phase 06) The Referee needs a "repeated the same ability" condition and Black Knight needs `hasTag` (OQ-31b) to detect Legends; neither exists. | Add both generically when batch 2/3 reaches those characters. | **Resolved (batch 3, ADR-015):** `repeatedAbility` and runtime `tags` were added generically.~~
| OQ-41 | ~~No implemented character applies Burn.~~ **Resolved (batch 2):** the Nine-Tailed Trickster's Fox Fire applies Burn, and a real-character test covers Hydra's Regrow being blocked. | A Burn user will arrive in later batches (Baba Yaga/Zeiron). |
| OQ-42 | (Phase 06) A Nameless One rewind returns both players to planning for the same turn, so each learns what the other played. Fine in hotseat/bot play. | Friend matches (Phase 09+) must treat a rewound turn as a new commit-reveal round, since the first reveal is public information. |
| OQ-43 | (Phase 06) Ability Lock holds one ability per target (`getActiveStatus` reads the first), so a second Revoke replaces the first rather than stacking. `@lastUsed` also resolves to the previous turn's ability when the target has not acted yet this turn (initiative order). | Both are deliberate limits; revisit in balance if Emperor Zero proves too weak or too swingy. |
| OQ-44 | ~~(Phase 07) The second player wins ~64% of decided games. Confirmed cause: OQ-03's "no energy on turn 1 for the initiative player". With the skip off the first player wins 49.5%. | Recommendation only: drop or soften the skip. Needs a decision (changes a balance rule).~~ **Resolved (Phase 08, owner delegated):** the turn-1 skip is off; first player now wins 50.3%. |
| OQ-45 | ~~(Phase 07) Energy starvation: with 1 random energy per living character, 67% of sampled INTERMEDIATE matches contain a team with no legal action for 3 turns running. Expensive kits (Zeiron, Emperor Zero) score worst. | Consider a higher base rate or catch-up rule; balance pass.~~ **Resolved (Phase 08):** 2 energy per living character; lockout hits down 68%, matches 36% shorter. |
| OQ-46 | (Phase 07) Patient Zero (74%) and Hydra (68-78%) look dominant at every bot level; Maestro Nocturne (about 34%) looks weak. Bots may under-rate combo kits. | Investigate causes before changing numbers; re-run with stronger bots. |
| OQ-47 | (Phase 07) No bot difficulty selector in the UI yet, and Legend-boss hooks are a framework with no encounter. | Phase 08 UI and later PvE work. |
| OQ-48 | (Phase 08 sim) After the energy change: Black Knight wins 66-70% and The Nameless One about 60%; Patient Zero stays at 74-77%; Maestro Nocturne, The Referee, Moonshot Maddox, Malachar sit at 34-38%. | Recommendations only; investigate causes before changing numbers. |
| OQ-49 | (Phase 08) Codex passive discovery is a heuristic: a discoverable passive is revealed when an event type its effects produce is logged with its holder as source or target, so an ability that causes the same event type can reveal it early. | Acceptable for now; a precise "fired by passive" marker on engine events would remove the ambiguity. |
| OQ-50 | (Phase 08) The Legend Chamber can only wake the six implemented Legends; the other six positions are locked symbols until they are built. The Nameless One needs all eleven others, so it cannot open yet. | Expected until the remaining six Legends exist. |
| OQ-51 | (Phase 09) The phase asks for Playwright tests (fresh profile, play, reload, transfer between two browser contexts) and a Lighthouse PWA check. Playwright is not installed: it needs browser downloads in CI. The same flows are covered with jsdom plus fake-indexeddb integration tests, and the real IndexedDB and QR path was checked by hand in the browser preview. | Add Playwright (with an offline test and Lighthouse) as its own CI job in the polish phase. |
| OQ-52 | (Phase 09) Transfer codes clamp Codex pair counts to 3 and omit history/replays by design; a JSON backup is the lossless path. | Accepted. |
| OQ-53 | (Phase 09) Legend trials use the LEGEND_BOSS bot without any rule bending; the hooks exist but no encounter uses them. Trial teams (Legend plus two allies) are a first guess and untuned. Trial difficulty against EXPERT-level play is unmeasured. | Simulate trials and tune in the balance pass. |
| OQ-54 | (Phase 09) XP values, mission goals and rewards are first-pass numbers. The camera scanner on real devices (iOS especially) is untested here. | Balance pass and device testing. |
| OQ-55 | (Phase 09) Only six of twelve Legends exist, so the eleven-Legend gate for the Nameless One cannot be met in play yet; it is tested with synthetic profiles. | Expected until the rest are built. |
| OQ-56 | (Phase 10) Friend matches are honor-based in two ways no server-less design can close: the last player to reveal can abort to avoid a bad outcome, and "own unlocks" cannot be verified. Both are stated in the UI. | Accepted; matches are meant for friends. |
| OQ-57 | (Phase 10) There is no turn timer or timeout in friend matches (they are asynchronous), so a friend who never replies leaves the match open forever. | Resign or remove it from the list; consider an optional agreed deadline later. |
| OQ-58 | (Phase 10) The RNG is a 32-bit state, so the seed and the mixed per-turn state are 32 bits. Fine for fairness between friends, not a cryptographic guarantee. | Accepted. |
| OQ-59 | (Phase 10) Live WebRTC (stretch) was not built. The game has not been tested across two real devices or browsers; two-client behavior is covered by simulation and by UI tests with two separate stores. | Manual two-device test, then Playwright with two contexts (see OQ-51). |
| OQ-60 | (Phase 11) The ladder's opponents come from a measured meta pool that inherits OQ-48: at high divisions bots lean on Patient Zero and the Black Knight, so Diamond and above will feel repetitive until balance changes. | Rerun `pnpm meta` after any balance change; consider a cap on how often one fighter appears. |
| OQ-61 | (Phase 11) The ladder has no real players to calibrate against. K values (40/24), the 100-point tiers, the 20-point demotion buffer and the halved season reset are reasonable guesses tested with simulated players only. | Tune from real play; all numbers are constants at the top of `packages/persistence/src/ranked.ts`. |
| OQ-62 | (Phase 11) A player can edit their own save (accepted in spec/06) and can close the app between a match ending and the result being saved. A match left unfinished is counted as a loss, but this cannot be enforced against a determined editor. | Accepted; ranked is a private ladder. |
| OQ-63 | (Phase 11) Pick/ban is a two-step framework (four picks, one ban each). There is no time pressure, no ban of a whole role or Legend, and only Diamond and above use it. | Extend later if the meta calls for it. |
| OQ-64 | (Phase 11) Season length is a calendar month in UTC; a player's local month boundary can differ by up to a day. | Accepted. |
| OQ-65 | (Phase 12) No balance version has been published yet, so new matches still record `BALANCE_VERSION_ID = "phase-05-v1"` and UI text (tooltips, Codex) reads the shipped libraries. When the first patch is added to `SHIPPED_BALANCE_PATCHES`, new matches and the UI need to point at the newest version. | Do it with the first published version (see docs/design/balance-workflow.md). |
| OQ-66 | (Phase 12) The workbench edits numbers only: it cannot add or remove effects, change targeting or rewrite a mechanic, by design ("no source editing"). Signed values (modifyResource, modifyEnergy, modifyCooldown amounts) are not listed. | Extend the tunable rules if a designer needs them. |
| OQ-67 | (Phase 12) Personal stats read the last 50 matches and the newest 20 replays, so "favourite abilities" describes recent play only; friend matches from before Phase 12 have no recorded side and count as side A. | Accepted. |
| OQ-68 | (Phase 12) The workbench was tested in jsdom (inline simulation) but its Worker path and layout were not exercised in a real browser. | Playwright pass with OQ-51. |
| OQ-69 | (Phase 12) The dev simulation uses the same INTERMEDIATE-bot random-team method as the CLI; small runs (the 300-match default) are noisy and only good for spotting big swings. | Use `pnpm sim` with 10,000 matches before acting on a finding. |
| OQ-70 | (Phase 13) **The Forgotten Titan wins 67%** in the 5k simulation after three tuning rounds; Asterion, Cyclops Brontes, The Bronze Giant and Nemesis sit at 63-64%. Heuristic bots are poor at killing a sleeper before it wakes, so this may partly be a bot artefact. | Phase 15: re-run at Expert level and with humans; consider a higher wake threshold or removing its Damage Reduction. |
| OQ-71 | (Phase 13) The new kits are simple and cost-efficient and pull older, conditional kits (Malachar, Referee, Maestro, Koschei, Moonshot) down to about 31-36%. | Phase 15 should raise the old kits as well as trim the new ones. |
| OQ-72 | (Phase 13) Petrification, Fear and Curse are marker statuses with no engine behaviour. Medusa composes petrification from Stun and Damage Reduction, Cerberus's Howl relies on Weakness, and Curse is only read by Dark Supper. | Give them real hooks if more characters need them. |
| OQ-73 | (Phase 13) Coverage of the required mechanics is not complete: energy generation, resurrection, pets, relics and ink have no character yet. | Later regions: a summoner or beastmaster (pets), an ink character in the Japanese region, a relic user. |
| OQ-74 | (Phase 13) Ability names and descriptions are read by the coverage script for flavor mechanics (fire, songs, and so on), so renaming an ability can silently drop a character from a row. | Acceptable; the region roster test would notice a loss for the mechanics it names. |
| OQ-75 | (Phase 13, region 2) Pets are bodies only: the Paper Crane Familiar takes damage and expires but has no actions, and the engine has no summon that acts or attacks. | Add acting summons (a summon's `abilityIds` are already in the schema) when a pet character needs them. |
| OQ-76 | (Phase 13, region 2) The Mirror Samurai (64%) and Gashadokuro (63%) are above the 60% flag; Oni of the Red Gate (36%), Kappa Kiro (39%) and The Painted Ronin (41%) are low. | Phase 15; re-run at Expert level before acting. |
| OQ-77 | (Phase 13) Transfer codes now carry only each pair table's strongest 300 entries, so a very heavy player loses their weakest pair statistics on a device transfer (the Codex reads counts of 2 or more only). Backups are complete. | Accepted; revisit if the Codex starts to use more pair data. |
| OQ-78 | (Phase 13, region 2) The resurrection ability has no visual "fallen" targeting in the match screen beyond highlighting the dead card; and the bots rarely choose it. | Playtest and polish in Phase 15. |
| OQ-79 | (Phase 13, region 2) "Relics" is the only required mechanic with no character. | A later region (Egypt / desert is a natural home). |
| OQ-80 | (Phase 13, region 3) Region 3's new kits average 58% against heuristic bots, more than regions 1 and 2, while the oldest conditional kits sit at 28-33%. Balance is drifting toward "simple and efficient". | Phase 15: raise the oldest kits as well as trimming the new ones; re-run at Expert level. |
| OQ-81 | (Phase 13, region 3) One-Eyed Likho (42%) is the low outlier; The Firebird (64%), Zmey Gorynych (62%), Marya (61%) and Domovoi (61%) are slightly above the flag. | Phase 15. |
| OQ-82 | (Phase 13, region 3) The Firebird's rebirth sets her health (a `setHp` heal), so Anti-Heal and Healing Reduction cannot stop it. Only erasure and two lethal hits in one turn beat it. | Accepted as counterplay; review with real players. |
| OQ-83 | (Phase 13, region 3) Relics are a resource, not objects: the Tsar's relics cannot be stolen or destroyed by the enemy. | See OQ-32 (targetable summons and objects). |

| OQ-84 | (Phase 13, region 4) The Valkyrie (65%), Shieldmaiden Yrsa (63%) and Draugr (62%) are above the 60% flag after tuning; Púca (38%) and Banshee (42%) are below 40-45%. | Phase 15. |
| OQ-85 | (Phase 13, region 4) Morrigan's prophecy is affliction damage and cannot be dodged with Invulnerable; the only counters are staying above half health, healing, dispel, or stopping her set-up. Needs playtesting for feel. | Review with real players. |
| OQ-86 | (Phase 13, region 4) Fear and Curse are still marker statuses with no engine hook (Banshee, The Dullahan). | Phase 15: give Fear a real rule (for example a chance to lose an action). |
| OQ-87 | (Phase 13, region 4) With 67 characters the ranked bots' Gold and Apex drafts average nearly the same measured win rate (56.1 vs 56.1), so the ladder test now only requires Apex > Bronze + 1 and Apex > Gold - 1. High divisions are not clearly stronger by character choice. | Phase 15: weight the meta pool by trio strength or raise the quality curve. |
| OQ-88 | (Phase 13, region 5) Anubian Judge (61%), The Living Sarcophagus (58%) and Ifrit (58%) sit above the 55-60% band after tuning; The Sphinx (44%) and Desert Djinn (45%) are low. | Phase 15. |
| OQ-89 | (Phase 13, region 5) Aurelia's Sun Guard holds against every damage type that reaches `applyHp`, including damage over time. A stalling team could hold allies at 1 health while healing; the counters are silence, erasure, dispel and a 4-turn cooldown. Needs playtesting for feel and stalling. | Review with real players. |
| OQ-90 | (Phase 13, region 5) Curse is still a marker with no rule (as in OQ-86); The Mummy Prince relies on Weakness for the effect. | Phase 15: give Curse a real rule. |
| OQ-91 | (Phase 13, region 5) At 78 characters a maxed profile no longer fit one QR code with 300 pairs per table (2954 > 2953 bytes), so `MAX_TRANSFER_PAIRS` is now 250; the favorites test used to open the alphabetically first fighter, which became a Secret silhouette (Anubian Judge), so it now picks Tortuga Rex. The transfer cap scales with the roster and will bite again near 120. | Phase 15: cap the transfer by byte budget instead of a pair count. |
| OQ-92 | (Phase 13, region 6) The Storyteller (61%) and The Thousand-Faced Stranger (60%) sit at the flag after tuning; The Wandering Genie (42%), Dokkaebi (44%) and The Moon Rabbit (45%) are low. | Phase 15. |
| OQ-93 | (Phase 13, region 6) `modifyEnergy` with the neutral family does nothing in the engine; only the four families hold energy. It is legal in the schema, so a designer could write a dead effect. | Phase 15: reject it in the schema or give neutral energy a meaning. |
| OQ-94 | (Phase 13, region 6) Madame Fortuna's rigging only matters for characters with random abilities and depends on branch order (worst to best). Nothing enforces the order, so a badly ordered outcome would make Fortune's Favour a curse. | Phase 15: add a per-branch `quality` hint or a content test that flags unordered branches. |
| OQ-95 | (Phase 13, region 6) At 90 characters even 250 pairs per table overflowed one QR code (3190 bytes), so `encodeTransfer` now halves-by-a-fifth the pair limit until the code fits (only about 40 pairs fit for a maxed profile); OQ-91 is resolved by this. A maxed-out profile therefore loses most of its "beaten X with Y" pair progress over a QR transfer (the JSON backup keeps everything). | Phase 15: shrink the payload (varint ids, dropping the pair tables from transfer entirely) or split across several QR codes. |
| OQ-96 | (Phase 13, region 7) The Marionettist (38%) and The Thing Beneath the Bed (44%) are low after a buff; The Scarecrow (58%) and The Collector (57%) are above 55%. Summoner and control kits are systematically weaker for heuristic bots. | Phase 15: re-run at Expert level before acting. |
| OQ-97 | (Phase 13, region 7) At 98 characters a maxed profile sends only about 5 pair-table entries in a transfer code (the fixed part of the payload now takes most of the QR budget), and the ladder's bot teams no longer rise steadily with division: Gold is about 1 point above Bronze and Apex is about 1 point BELOW Gold by measured win rate (the meta pool now has 20 top teams because fewer trios reach the minimum games; the test now requires Gold and Apex above Bronze by 0.5 and Apex within 2 of Gold). Both are effects of the roster growing while win rates compress towards 50%. | Phase 15: shrink the transfer payload (varint ids, no pair tables), and make bot team quality scale with the spread of the meta pool. |
| OQ-98 | (Phase 13, region 8) The Honey Badger (61%) is above the flag; The Albino Gorilla (47%) and Professor Octopus (45%) are low. The Minotaur King needed the largest nerf of any Legend (75% to 57%): a team-wide stun plus a taunt tank is very strong against heuristic bots. | Phase 15: re-run at Expert level, and watch the Legend for stun-lock feel. |
| OQ-99 | (Phase 13, region 8) Fixes to scale problems (OQ-95, OQ-97): a transfer code sends long id lists (the Codex) as bitsets over the id table (order becomes the table's order), and `pnpm meta` lowers its minimum games per trio floor from 6 to 3 (at 105 characters most trios are seen fewer than once in 60,000 matches; the smoothing toward 50% keeps a lucky sample from topping the list). The top-teams list is again 40 long. The ladder's Gold/Apex margins loosened in OQ-97 are unchanged. | Phase 15: sample trios deliberately instead of at random. |
| OQ-100 | (Phase 13, region 9) The Gunslinger QB (37%) is low even after better odds: its worst branch costs it 20 of 100 health, and heuristic bots do not weigh a gamble. | Phase 15: re-run at Expert level; consider a smaller self-cost. |
| OQ-101 | (Phase 13, region 10) Johnny Feedback (44%) and The Mime (44%) are low; DJ Cataclysm (59%) and Orpheon (58%) are high. Chef Ramble needed the largest nerf (77% to 55%) before the transformation stopped dominating. The engine cannot damage a whole allied team from one effect, so Johnny Feedback's "hurts allies" from spec/03 is not modelled. | Phase 15; consider an ally-all damage target if the spec detail matters. |
| OQ-102 | (Phase 13, region 11) A character has one passive slot, so Calypsa's Tide rises by a passive but does not wash away when she is wounded (the spec's flavour). | Phase 15: allow a second passive slot, or give the ebb to a status. |
| OQ-103 | (Phase 13, region 11) The Lawyer (65%) and The Gatekeeper (64%) are above the flag after two tuning rounds; The Tax Collector (46%) is low. Their passives (a restore-once, a toll while barred, a levy on audited turns) are efficient against heuristic bots. | Phase 15: re-run at Expert level. |
| OQ-104 | (Phase 14) Art `region` strings are free text with about 20 spellings (for example several Slavic variants), so the export groups characters for cultural review by keyword. A typo in a region string silently lands a character in the low-priority "original" group. | Phase 15: make `region` an enum of cultural groups with the free text as detail. |
| OQ-105 | (Phase 14) The audit checks prompts, not images: nothing verifies that generated art matches across regions. A human art director has to approve the four-character reference set before the batch run. | Before production art. |
| OQ-106 | (Phase 15) Offline was verified only in Chromium (the built-in browser). Firefox, WebKit (Safari), a real iPhone and a real Android device were not tested, and neither was Lighthouse or Playwright (no browsers or Playwright install in the development environment). Safari and iOS have their own service-worker and storage-eviction rules. | Before release: run the offline check on Firefox and WebKit, an iPhone (Add to Home Screen) and an Android device, and run Lighthouse. |
| OQ-107 | (Phase 15) The optional single-file build (open `index.html` from disk) was not built: the bot Web Worker and module scripts do not load from a `file:` URL without inlining. | Add `vite-plugin-singlefile` with an inlined worker if a local-file build is wanted. |
| OQ-108 | (Phase 15) A waiting service worker takes over when every old tab closes; the page does not yet show an "update ready, reload" prompt or send SKIP_WAITING. | Phase 15 follow-up: a small update banner. |
| OQ-109 | (Phase 15) The base balance version id is still `phase-05-v1` although the numbers changed in Phases 13 and 15, because nothing has shipped. Any replay recorded before this build replays under the new numbers. | Close OQ-65 and publish through `SHIPPED_BALANCE_PATCHES` before the first public release. |
| OQ-110 | (Phase 15) The remaining spread: about 2.7% of matches still have a team with no legal action for three turns (a last survivor with only very expensive abilities), The Firebird (67%), El Magnífico and The Birch Witch (over 62%) are high, Malachar (32%), Kappa Kiro, The Gunslinger QB, Moonshot Maddox, Oni of the Red Gate and Shiro (36-37%) are low, and 0.7% of ADVANCED matches contain a kill from full health in one turn. All are within what heuristic bots can show; EXPERT-level runs were not possible at scale. | Real playtests and an EXPERT-level batch on faster hardware. |
