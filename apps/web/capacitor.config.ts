import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.veilbreak.arena',
  appName: 'Veilbreak: Arena of the Fallen',
  webDir: 'dist'
};

export default config;
